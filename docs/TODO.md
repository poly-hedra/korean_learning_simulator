# TODO.md — 이성준 FE 작업 목록

> **MVP 제약**: 초급 사용자 × 한강 맵만 우선 구현
> **장소(맵)**: 확정된 목록 없음, 추후 추가 가능. 버튼은 데이터 기반 동적 렌더링으로 설계.
> 작업은 위에서 아래 순서로 진행합니다.
> 완료된 항목은 `- [x]`로 체크하고 worklog를 작성합니다.

---

## 🔴 Must Have

### SETUP | 프로젝트 초기 세팅

- [x] **01** Next.js 15 + TypeScript + Tailwind CSS 초기화 (`feat/이성준` 브랜치)
- [x] **02** 폴더 구조 생성 (`app/` / `components/` / `hooks/` / `types/`)
- [x] **03** 공통 디자인 토큰 정의 (`globals.css @theme` — Tailwind v4 방식)
  - 다크 배경 `#16140F`, Gold `#FAC775`, 주황 `#EF9F27`
  - 티어별 테두리 컬러 (Bronze / Silver / Gold / Platinum / Diamond)
- [x] **04** 전체 레이아웃 컴포넌트 구현 (`max-width: 480px`, `margin: 0 auto` — 모바일 웹 고정)

---

### F-HOME | 홈 화면 (Must)

- [ ] **05** 홈 페이지 라우트 생성 (`/`)
- [ ] **06** `HomeHeader.tsx` — 앱 이름 "코대헌" + 영문 서브타이틀 / 우측 프로필 원형 아이콘 + @유저ID / 티어값에 따른 테두리 컬러 동적 적용 _(BE 연동: 유저 프로필 API)_
- [ ] **07** `TierCard.tsx` — 티어명 / 등급 / XP 현재·최대 / 다음 티어까지 남은 XP 표시 _(BE 연동: XP 데이터 API)_
- [ ] **08** `WeeklyStats.tsx` — 대화 수 / 평균 점수 / 연속 학습일(스트릭) 3칸 가로 배열 / 스트릭 빨간색 강조 _(BE 연동: 주간 통계 API)_
- [ ] **09** 대화 시작 CTA 버튼 — 클릭 시 `/location` 이동 / scale 눌림 애니메이션
- [ ] **10** `BottomTabBar.tsx` — 홈/대화/기록/복습/내정보 5탭 / 아이콘+텍스트 / 활성 탭 주황·비활성 회색 / 현재 경로 감지 자동 표시
- [ ] **11** 탭별 라우팅 연결 (`/` / `/chat` / `/history` / `/review` / `/profile`)

> ⚡ BE API 완성 전까지 mock data로 UI 먼저 완성
> 🔗 **이강산에게 요청 필요**: 유저 프로필+XP+티어 API / 주간 통계 API

---

### F-01 | 온보딩 UI

- [ ] **12** 온보딩 페이지 라우트 생성 (`/onboarding`)
- [ ] **13** 국적 선택 드롭다운 UI
  - 자주 쓰는 국가 최상단 고정 배치
  - 드롭다운 내 타이핑으로 국가 검색·자동완성
  - IP 기반 자동 국가 선택 (FE에서 ipapi.co 직접 호출로 처리)
- [ ] **14** 한국어 수준 선택 UI (초급 / 중급 / 고급 버튼)
- [ ] **15** 가장 관심있는 한국 문화 선택 UI (1가지, 버튼식)
  - K-Content / K-Pop / K-Beauty / K-Food / K-Gaming·eSports / Others
- [ ] **16** 가보고 싶은 곳 선택 UI (1가지, 버튼식)
  - 현재 제공 장소: 한강 / 명동 / 롯데월드
  - ※ 장소 목록 미확정. 버튼은 데이터 배열 기반으로 동적 렌더링 (추후 항목 추가 용이하게)
- [ ] **17** 즉시 시작 팝업 — "이 장소로 대화를 바로 시작하시겠습니까?"
  - YES → 시나리오 생성 API 호출 → 역할(A/B) 선택 화면으로 이동
  - NO → 메인 화면(`/`)으로 이동 + 튜토리얼 팝업 실행
- [ ] **18** 입력값 유효성 검사 로직 작성 + 테스트 **(TDD)**
- [ ] **19** 완료 시 로컬스토리지에 사용자 프로필 저장
- [ ] **20** 2회차 접속 시 온보딩 건너뛰고 홈으로 자동 이동

---

### F-TUT | 튜토리얼 (최초 1회, 온보딩 NO 선택 후 실행)

- [ ] **21** 튜토리얼 완료 여부 로컬스토리지 저장 로직 (`tutorialDone: true`)
- [ ] **22** 어두운 반투명 오버레이 + 설명할 요소만 밝게 뚫는 스포트라이트 컴포넌트 구현
- [ ] **23** 8단계 순서 및 말풍선 텍스트 구현
  - STEP 1: 티어 카드 → "현재 내 레벨이에요"
  - STEP 2: 주간 통계 → "이번 주 학습 현황이에요"
  - STEP 3: 스트릭 → "매일 접속하면 스트릭이 올라가요"
  - STEP 4: 대화 시작 버튼 → "여기서 오늘의 대화를 시작해요"
  - STEP 5: 재도전 카드 → "아쉬웠던 대화, 다시 도전해봐요"
  - STEP 6: 복습 배너 → "퀴즈와 플래시 카드로 복습해요"
  - STEP 7: 하단 탭바 → "5개 탭으로 이동할 수 있어요"
  - STEP 8: 프로필 아이콘 → "내 프로필이에요"
  - 마지막: "준비됐어요! 첫 대화를 시작해볼까요? 🎉" → 장소 선택으로 이동
- [ ] **24** 상단 진행 표시 (예: 3 / 8) + 우상단 "건너뛰기" 버튼 항상 노출

---

### F-02a | 장소 선택 UI

- [ ] **25** 장소 선택 페이지 라우트 생성 (`/location`)
- [ ] **26** 장소 버튼을 데이터 배열 기반으로 동적 렌더링 (장소 추가 시 배열만 수정하면 되도록)
- [ ] **27** MVP 단계: 한강 버튼만 활성화, 나머지 비활성(흐리게) 처리
- [ ] **28** 장소 선택 시 시각적 활성화 상태 표시 (선택된 버튼 강조)
- [ ] **29** 선택 완료 → 시나리오 생성 API 호출 → 역할 선택 화면(`/persona`)으로 이동

---

### F-PERSONA | 역할(페르소나) 선택 화면

- [ ] **30** 역할 선택 페이지 라우트 생성 (`/persona`)
- [ ] **31** AI가 생성한 페르소나 A/B 카드 표시 (이름 / 직업 / 나이 / 성별 / 대화 목적)
- [ ] **32** 사용자가 A 또는 B 선택 → 채팅 화면으로 이동

---

### F-03 | 메신저형 채팅 UI

- [ ] **33** 채팅 페이지 라우트 생성 (`/chat`)
- [ ] **34** AI 페르소나 프로필 카드 컴포넌트 (이름 / 직업 / 나이 / 프로필 이미지)
- [ ] **35** 말풍선형 채팅 메시지 컴포넌트 (내 메시지 우측 / AI 메시지 좌측 구분)
- [ ] **36** 메시지 입력창 + 전송 버튼 UI
- [ ] **37** AI 응답 스트리밍 표시 (글자가 순서대로 나타나는 효과)
- [ ] **38** 남은 턴 수 표시 UI (예: "남은 대화 5 / 7")
- [ ] **39** 대화 종료 감지 후 결과 페이지(`/result`)로 자동 이동

---

### F-04 | 결과 & 점수 화면

- [ ] **40** 결과 페이지 라우트 생성 (`/result`)
- [ ] **41** 총점 (10점 만점) 표시 UI
- [ ] **42** 티어 표시 UI
- [ ] **43** 피드백 페이지로 이동하는 버튼

---

## 🟡 Should Have

### F-05a | 피드백 UI

- [ ] **44** 피드백 페이지 라우트 생성 (`/feedback`)
- [ ] **45** 대화 로그 다시보기 UI (스크롤 가능한 대화 목록)
- [ ] **46** 사용자 발화 중 오답 부분 빨간 음영 표시
- [ ] **47** 틀린 단어 목록 + 뜻풀이 표시
- [ ] **48** 대화 요약 피드백 텍스트 표시

---

### F-05b | 방사형 그래프

- [ ] **49** 방사형(레이더) 그래프 라이브러리 선택 및 설치
- [ ] **50** 어휘(30%) / 상황(50%) / 문법(20%) 3축 그래프 구현
- [ ] **51** 평가 에이전트 결과값(어휘/상황/문법 점수) 받아서 그래프 3축에 표시

---

### F-06a | 초성 퀴즈 UI

- [ ] **52** 주간 복습 페이지 라우트 생성 (`/review`)
- [ ] **53** 초성 퀴즈 카드 컴포넌트 (문제 + 4지선다 보기)
- [ ] **54** 정답 선택 시 정오답 피드백 UI (정답: 초록 / 오답: 빨강)
- [ ] **55** 퀴즈 완료 후 결과 요약 표시

---

### F-HOME-S | 홈 화면 Should Have

- [ ] **56** XP 진행 바 애니메이션 (화면 진입 시 0%→실제 비율까지 채워짐)
- [ ] **57** `RetryCard.tsx` — 최저점 시나리오명 + 점수 뱃지 + 재도전 버튼 / 왼쪽 주황 포인트 바 _(BE 연동: 최저점 세션 API)_
- [ ] **58** 재도전 버튼 클릭 시 해당 시나리오로 바로 진입 (장소 선택 스킵)
- [ ] **59** `ReviewBanner.tsx` — 초성 퀴즈 N개 + 플래시 카드 N개 표시 / 0이면 배너 숨김 _(BE 연동: 복습 항목 수 API)_

> 🔗 **이강산에게 요청 필요**: 최저점 세션 API / 복습 항목 수 API

---

## 🟢 Could Have

### F-05c | Scaffolding 팝업

- [ ] **60** AI 발화 말풍선 클릭 시 팝업 표시
- [ ] **61** 팝업 내 어절 단위 한→영 해석 표시

---

### F-06b | 플래시 카드 UI

- [ ] **62** 플래시 카드 컴포넌트 (앞: 한국어 / 뒤: 영어 + 예문)
- [ ] **63** 카드 뒤집기 애니메이션
- [ ] **64** "알았어요 / 모르겠어요" 버튼으로 복습 우선순위 조정

---

### F-HOME-C | 홈 화면 Could Have

- [ ] **65** 화면 진입 시 카드 순서대로 페이드인 애니메이션
- [ ] **66** 스트릭 마일스톤 달성 시 축하 토스트 메시지 (7일·30일 달성 시)
- [ ] **67** 스켈레톤 로딩 UI (API 응답 전 회색 박스로 레이아웃 유지)

---

### F-PROFILE | 내정보 탭

- [ ] **68** 프로필 페이지 라우트 생성 (`/profile`)
- [ ] **69** 레벨·국적·설정 정보 표시
- [ ] **70** "튜토리얼 다시 보기" 버튼 (tutorialDone 초기화 후 실행)

---

## 📝 작업 규칙 리마인더

- 한 번에 하나의 TODO만 작업
- 완료 시 `worklog/` 폴더에 md 파일 생성
- 파일명 = 커밋 메시지 (예: `003-장소-선택-버튼-UI-구현.md`)
- 확인 받은 후 다음 TODO로 이동
- BE API 미완성 항목은 mock data로 UI 먼저 완성 후 나중에 교체
